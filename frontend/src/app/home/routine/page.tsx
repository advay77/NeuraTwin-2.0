import RoutineTracker from "@/components/RoutineTrack";
import React from "react";

const page = () => {
  return (
    <section>
      <RoutineTracker />
    </section>
  );
};

export default page;
